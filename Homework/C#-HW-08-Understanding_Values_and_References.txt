C# Programming Homework 08 Chapter 08, C# Step by Step Readings Read chapter 8 in the C# Step by Step book. Discussion Questions Answer the discussion questions in writing. 

1. What is the difference between deep copy and shallow copy? 
simply copies references, is known as a shallow copy
copies every field and method into a completely new variable is a deep copy

2. What is the value of a reference after you declare and initialize it? 
The value points to a place in memory

3. How do you declare a value type? 
Int Var1 = 0;

4. How do you declare a reference type? 
Swan brandon = new Swan();

5. Does C# allow you to assign NULL to a value type? 
This way int? alex; No, YOU NEED A VALUE IN THAT LOCATION
You can assign the null value to any reference variable. 

6. Can you assign a nullable value type to a non-nullable variable of the same type? Why or why not? 
No, a nullable value type cannot assign nullable value types to a NON nullable value type.

7. What is the difference between the stack and the heap? 
The stack is ordered memory and the heap is unordered memory

8. What does it mean when we say that all classes are specialized types? 
Because an interation of the class is its own type, with its own unique name, added functionality

9. What does ref do? 
the C# compiler generates code that passes a reference to the actual argument rather than a copy of the argument.

10. What does out do? 
You can prefix a parameter with the out keyword so that the parameter becomes an alias for the argument.


